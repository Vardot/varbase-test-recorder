/**
 * Feature file generator — NOT USED with Playwright Test.
 * With pure Playwright, test logic lives entirely in .spec.js files.
 * This module is kept for backward compatibility with the UI layer.
 */

/**
 * @returns {null} Feature files are not generated for Playwright Test.
 */
export function generateFeatureFile() {
  return null;
}
